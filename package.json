{
  "name": "temp-updater",
  "version": "1.0.0",
  "description": "Google Sheets temperature/humidity chart updater",
  "scripts": {
    "setup": "node scripts/setup-clasp.js",
    "build": "node scripts/generate-config.js && tsc && node scripts/generate-config.js && cp appsscript.json dist/ && cp src/sidebar.html dist/",
    "watch": "tsc --watch",
    "push": "npm run build && clasp push && echo '\n✓ プッシュ完了\n→ GASエディタで setupConfig 関数を実行して設定を保存してください\n→ GASエディタを開く: npx clasp open\n'",
    "deploy": "npm run build && clasp push && clasp deploy",
    "prepare": "husky"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83",
    "husky": "^9.1.7",
    "typescript": "^5.3.3"
  }
}
